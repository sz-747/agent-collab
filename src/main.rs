//! agent-collab: git-transport two-human planning room with an @vice AI scribe.
//!
//! v1 is an all-Rust thin text face. Subcommands (`join` / `start`) are wired in
//! U7; this skeleton keeps the binary building while units land. The engine
//! itself lives in the library crate (`src/lib.rs`).

fn main() {
    eprintln!("agent-collab v1 (scaffold) — subcommands wired in U7");
}
